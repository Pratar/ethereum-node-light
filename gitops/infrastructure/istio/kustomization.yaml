---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: istio-gateway-api
  namespace: istio-system

resources:
  - istio-namespace.yaml
  - istio-gateway-controller.yaml
  - istio-ingressgateway.yaml

labels:
  - pairs:
      app.kubernetes.io/component: istio-gateway-api
      app.kubernetes.io/instance: base
      app.kubernetes.io/version: v1.20.0
